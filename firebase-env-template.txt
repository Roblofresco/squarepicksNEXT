# Firebase Environment Variables Template
# Copy this to .env.local and fill in your actual values

# Firebase Configuration
# Get these values from Firebase Console > Project Settings > General > Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Firebase Admin SDK (for server-side operations)
# Path to your service account JSON file
GOOGLE_APPLICATION_CREDENTIALS=./certificates/firebase-admin-key.json

# App Check (optional - for additional security)
NEXT_PUBLIC_APPCHECK_DEBUG=true
NEXT_PUBLIC_RECAPTCHA_V3_SITE_KEY=your_recaptcha_site_key

# PayPal Configuration (if using PayPal integration)
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id
PAYPAL_CLIENT_SECRET=your_paypal_client_secret

# Stripe Configuration (if using Stripe integration)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=your_stripe_publishable_key
STRIPE_SECRET_KEY=your_stripe_secret_key
